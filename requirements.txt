# NOTE: install the CUDA build of torch separately so pip does not pull the
# CPU-only wheel, e.g.:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu124
torch>=2.1.0
# Qwen3-VL requires transformers >= 4.46 (AutoModelForImageTextToText + dtype=);
# 4.49+ recommended for full Qwen3-VL support.
transformers>=4.49.0
accelerate>=0.25.0
Pillow>=10.0.0
networkx>=3.1
matplotlib>=3.7.0
numpy>=1.24.0
tqdm>=4.65.0
huggingface_hub>=0.15.0
qwen-vl-utils>=0.0.14
torchvision>=0.16.0